from django.contrib import admin

# Register your models here.


# 1. Register your models here.
# 2. Run `python manage.py makemigrations` to create the necessary migration files.
# 3. Run `python manage.py migrate` to apply the migrations to the database.
# 4. Start the development server with `python manage.py runserver`.


# Example: Registering a custom model.
